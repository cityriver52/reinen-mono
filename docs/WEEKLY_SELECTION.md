# Weekly Chat selection

通常の週次Chat通知は最大3件。

## 選定

- **0〜7日後**: `score` 降順で最大2件
- **8〜21日後**: `score` 降順で最大1件
- `score` が同点なら開始時期が近いものを優先
- `今年は不要` / スヌーズ中は除外
- 過去にChatへ表示済みかどうかは順位に影響させない

そのため、重要な案件が翌週も対象期間に残っていれば、2週連続で表示される。これは意図したリマインド挙動。

開始時期を過ぎた案件の通知は、この通常3件とは別枠で扱う。
